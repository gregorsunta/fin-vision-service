{
  "watch": ["src"],
  "ext": "ts",
  "exec": "tsx src/api/index.ts",
  "events": {
    "start": "echo 'Starting API server with nodemon...'",
    "crash": "echo 'API server crashed. Restarting...'",
    "restart": "echo 'API server restarting due to changes...'"
  }
}
